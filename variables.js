export default function domVariables() {
    const listsGroup = document.getElementById("lists-group");
    const newList = document.createElement("div");
    const listTitle = document.createElement("input");
}
